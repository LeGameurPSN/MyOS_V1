import QtQuick 2.0

Rectangle {
    color: "#0a0a12"
    anchors.fill: parent

    Text {
        anchors.centerIn: parent
        text: "Bienvenue sur PSN_OS — installation en cours..."
        color: "#ffffff"
        font.pixelSize: 24
    }
}
